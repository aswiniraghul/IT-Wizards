const HOST_NAME = "http://localhost:8080";

const USER_PATH = "/api/users";

const SIGNUP_API = `${HOST_NAME}${USER_PATH}/signup`;

const LOGIN_API = `${HOST_NAME}${USER_PATH}/signin`;

const USERS_API = `${HOST_NAME}${USER_PATH}`;

export { HOST_NAME, USER_PATH, LOGIN_API, USERS_API, SIGNUP_API };